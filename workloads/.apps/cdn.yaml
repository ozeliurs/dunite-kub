apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cdn
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ozeliurs/dunite-kub.git
    targetRevision: HEAD
    path: workloads/apps/cdn
  destination:
    server: https://kubernetes.default.svc
    namespace: cdn
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
